{
  "private": true,
  "dependencies": {
    "del-cli": "^4.0.1",
    "parcel": "^2.0.0",
    "parcel-transformer-rough-svg": "file:../../"
  },
  "scripts": {
    "build": "parcel build --no-cache --no-optimize index.html",
    "clean": "del .parcel-cache dist"
  }
}
